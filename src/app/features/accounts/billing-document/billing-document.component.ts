import {Component,inject} from '@angular/core';import {CommonModule} from '@angular/common';import {ActivatedRoute,RouterLink} from '@angular/router';import {CrmStore} from '../../../core/services/crm-store.service';import {PermissionService} from '../../../core/services/permission.service';
@Component({selector:'app-billing-document',standalone:true,imports:[CommonModule,RouterLink],templateUrl:'./billing-document.component.html',styleUrl:'./billing-document.component.css'})
export class BillingDocumentComponent {store=inject(CrmStore);access=inject(PermissionService);route=inject(ActivatedRoute);kind=this.route.snapshot.data['resource'];get record(){const item=this.store.list(this.kind).find(r=>r.id===this.route.snapshot.paramMap.get('id'));return item&&this.access.visible(this.kind,item,this.store.records())?item:null}get invoice(){return this.kind==='payments'?this.store.list('invoices').find(i=>i.id===this.record?.invoiceId):this.record}get customer(){return this.store.list('customers').find(c=>c.id===(this.record?.customerId||this.invoice?.customerId))||this.store.list('leads').find(l=>l.id===this.record?.leadId)}get company(){return this.store.list('billingProfile').find(c=>c.status==='ACTIVE')||{name:'AMI HUB'}}get title(){return this.kind==='payments'?'Payment receipt':this.kind==='quotations'?'Quotation':'Invoice'}print(){window.print()}}
