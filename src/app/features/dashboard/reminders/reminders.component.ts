import {Component,inject} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {RouterLink} from '@angular/router';import {ReminderService} from '../../../core/services/reminder.service';
@Component({selector:'app-reminders',standalone:true,imports:[CommonModule,FormsModule,RouterLink],templateUrl:'./reminders.component.html',styleUrl:'./reminders.component.css'})
export class RemindersComponent {service=inject(ReminderService);filter='all';query='';get rows(){return this.service.items.filter(r=>(this.filter==='all'||this.filter==='overdue'&&r.days<0&&r.kind!=='Payment received'||this.filter==='today'&&r.days===0||this.filter==='upcoming'&&r.days>0||this.filter==='unread'&&!r.read)&&(!this.query||(r.title+' '+r.detail).toLowerCase().includes(this.query.toLowerCase())))}get overdue(){return this.service.items.filter(r=>r.days<0&&r.kind!=='Payment received').length}get today(){return this.service.items.filter(r=>r.days===0).length}}
